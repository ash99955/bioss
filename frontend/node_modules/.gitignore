cd "C:\Users\ash99\OneDrive\Bioink\Software\bioss"

# Append the line only if it's not already there
if (-not (Select-String -Path .gitignore -Pattern 'frontend/node_modules/' -Quiet)) {
    'frontend/node_modules/' | Add-Content .gitignore
}
